# E2E Test Environment Configuration
#
# Copy this file to .env.e2e and customize as needed.
# These values are used by the Playwright tests and start-services.sh script.

# Webapp URL (Vite dev server)
WEBAPP_URL=http://localhost:5173

# gridapi URL
GRIDAPI_URL=http://localhost:8080

# Keycloak configuration
KEYCLOAK_URL=http://localhost:8443
KEYCLOAK_REALM=grid

# Database connection
DATABASE_URL=postgres://grid:gridpass@localhost:5432/grid?sslmode=disable

# External IDP configuration (Mode 1 - Keycloak as external provider)
EXTERNAL_IDP_ISSUER=http://localhost:8443/realms/grid
EXTERNAL_IDP_CLIENT_ID=grid-api
EXTERNAL_IDP_CLIENT_SECRET=tsREgTe21npWljPNsYf6qzenc2AWF9e9
EXTERNAL_IDP_REDIRECT_URI=http://localhost:8080/auth/sso/callback

# Test user credentials
TEST_USER_EMAIL=alice@example.com
TEST_USER_PASSWORD=test123

# Playwright configuration
# Set to 1 to run tests in headed mode (see browser UI)
HEADED=0

# Set to 1 to enable debug mode
DEBUG=0

# Set to 1 to generate traces for all tests
TRACE=0
